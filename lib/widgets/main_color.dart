import 'package:flutter/material.dart';

const kSecondaryColor = Color(0xFF76398e);
const kGrayColor = Color(0xFF1d5bd7);
const kpinkColor = Color(0xFFddd6f3);
const kColor = Color(0xFF886c93);
const kBColor = Color(0xFF8B94BC);

const kPrimaryGradient = LinearGradient(
  colors: [Color(0xFFC1C1C1), Color(0xFF8B94BC), Color(0xFF00FFCB)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const primaryColor = Color(0xFF020024);
const blueColor = Color(0xFF090979);
const tealColor = Color(0Xff00d4ff);

const kpurpleColor = Color(0xFF8B94BC);

const kwhitepinkColor = Color(0xFFC1C1C1);
const kBlackColor = Color(0xFF101010);
const PrimaryGradient = LinearGradient(
  colors: [Color(0xFFC1C1C1), Color(0xFF8B94BC), Color(0xFF00FFCB)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const primaryBGcolor = LinearGradient(
  colors: [
    Color(0xFF020024),
    Color(0xFF090979),
    Color(0Xff00d4ff),
  ],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const double kDefaultPadding = 20.0;
